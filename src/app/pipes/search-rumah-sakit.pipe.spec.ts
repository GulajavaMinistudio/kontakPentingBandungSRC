import { SearchRumahSakitPipe } from './search-rumah-sakit.pipe';

describe('SearchRumahSakitPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRumahSakitPipe();
    expect(pipe).toBeTruthy();
  });
});
